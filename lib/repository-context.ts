import { getContents } from "./github";
const priority=["README.md","package.json","pnpm-lock.yaml","yarn.lock","package-lock.json","requirements.txt","pyproject.toml","go.mod","Cargo.toml","Dockerfile","docker-compose.yml",".env.example","tsconfig.json"];
const sourceExt=/\.(ts|tsx|js|jsx|py|go|rs|java|kt|rb|php|cs)$/;
export async function buildRepositoryContext(owner:string,name:string){const root=await getContents(owner,name);if(!Array.isArray(root))return "";const files=root.map((x:any)=>x.path).filter((p:string)=>!p.includes("node_modules")&&!p.startsWith(".git/"));const selected=[...priority.filter(p=>files.includes(p)),...files.filter(p=>sourceExt.test(p)&&p.split("/").length<=3).slice(0,35)];const chunks:string[]=[];for(const path of selected){const data=await getContents(owner,name,path);if(data?.content){const text=Buffer.from(data.content,"base64").toString("utf8").slice(0,12000);chunks.push(`FILE: ${path}\n${text}`)}}return chunks.join("\n\n").slice(0,90000)}
