"use client";
import { useState } from "react";
import { ArrowRight, Github, Sparkles } from "lucide-react";
export default function RepoInput({onAnalyze}:{onAnalyze:(url:string,demo:boolean)=>void}){const [url,setUrl]=useState("");return <div className="w-full max-w-3xl"><div className="glass flex flex-col gap-3 rounded-2xl p-2 shadow-glow sm:flex-row"><div className="flex flex-1 items-center gap-3 px-4"><Github size={20} className="text-muted"/><input value={url} onChange={e=>setUrl(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")onAnalyze(url,false)}} placeholder="https://github.com/user/project" className="w-full bg-transparent py-3 text-sm outline-none placeholder:text-muted"/></div><button onClick={()=>onAnalyze(url,false)} className="flex items-center justify-center gap-2 rounded-xl bg-white px-5 py-3 text-sm font-semibold text-black hover:bg-zinc-200">Analyze Repository <ArrowRight size={16}/></button></div><button onClick={()=>onAnalyze("",true)} className="mt-3 flex items-center gap-2 px-2 text-sm text-muted hover:text-white"><Sparkles size={15}/> Try Demo</button></div>}
