export default function MetricCard({label,score}:{label:string;score:number}){return <div className="rounded-xl border border-line bg-[#0d0f13] p-4"><div className="flex items-center justify-between"><span className="text-xs uppercase tracking-wider text-muted">{label}</span><span className="text-lg font-semibold">{score}</span></div><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-zinc-800"><div className="h-full rounded-full bg-violet-400" style={{width:`${score}%`}}/></div><p className="mt-2 text-xs text-muted">{score>=80?"Strong foundation":score>=60?"Needs attention":"Priority gap"}</p></div>}
